/* ============================================================
   GOOGLE SHEET CREDENTIALS
   ------------------------------------------------------------
   This is the ONLY file you need to edit to connect (or change)
   the Google Sheet that enquiry data is saved to.

   How to get the Web App URL:
     1. Open GOOGLE_SHEET_SETUP.md (in this folder) and follow the
        step-by-step guide to create the Google Apps Script.
     2. After you "Deploy" the Apps Script as a Web App, Google gives
        you a URL that ends with "/exec".
     3. Paste that URL below, between the quotes.

   To switch to a different sheet later, just replace the URL here.
   ============================================================ */

const GOOGLE_SHEET_CONFIG = {
    // Paste your deployed Apps Script Web App URL here (must end with /exec)
    webAppUrl: ""
};
